#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n,m;
    int cnt = 0;
    cin >> n >> m;
    while (n > 0 && m > 0){
        
        
        cout << "Case " << ++cnt << ": " << ans << '\n';
        cin >> n >> m;
    }
    return 0;
}