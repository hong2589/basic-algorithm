#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    int u, v;
    vector<int> adj[51];
    cin >> n;
    cin >> u >> v;
    while (u > 0 && v > 0){
        
        cin >> u >> v;
    }
}